# Secrets — every service reads its own .env (never committed)
.env
**/.env

# Local data / logs (bind-mounted under each service dir)
**/clickhouse_data/
**/clickhouse_logs/
**/valkey_data/

# Rust build artifacts (ingest-api)
**/target/

# Editor / OS noise
*.bak
.DS_Store
